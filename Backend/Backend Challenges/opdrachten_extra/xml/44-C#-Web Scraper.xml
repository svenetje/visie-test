<Assignment xmlns="http://schemas.datacontract.org/2004/07/Syn_OpenAI_AppLib.Data" xmlns:i="http://www.w3.org/2001/XMLSchema-instance"><AssignmentText>Opdracht: Web Scraper

Titel: Web Scraper voor nieuws van NASA of ESA

Doel: Maak een eenvoudige web scraper die nieuwsartikelen van NASA of ESA ophaalt en de titel, publicatiedatum en samenvatting van elk artikel weergeeft.

Programmeertaal: C#

Moeilijkheidsgraad: Makkelijk

Inleiding: In deze opdracht ga je een eenvoudige web scraper bouwen die nieuwsartikelen van NASA of ESA verzamelt. Een web scraper is een programma dat gegevens van websites haalt en de verkregen gegevens structureert en opslaat voor analyse of andere doeleinden. Je gaat gebruikmaken van C# en de HtmlAgilityPack library om dit te realiseren.

Benodigdheden:
1. Visual Studio of een andere C#-ontwikkelomgeving.
2. HtmlAgilityPack (https://www.nuget.org/packages/HtmlAgilityPack/)

Stappen:
1. Maak een nieuw C# Console Application-project in Visual Studio.
   - Open Visual Studio en kies voor 'Create a new project'.
   - Zoek naar 'Console App (.NET Core)' en selecteer deze optie. Klik vervolgens op 'Next'.
   - Geef je project een naam en kies een locatie waar je het wilt opslaan. Klik op 'Create' om het project aan te maken.

2. Installeer HtmlAgilityPack via NuGet Package Manager.
   - Ga naar 'Tools' &gt; 'NuGet Package Manager' &gt; 'Manage NuGet Packages for Solution'.
   - Klik op 'Browse' en zoek naar 'HtmlAgilityPack'.
   - Selecteer 'HtmlAgilityPack' en installeer het in je project.

3. Gebruik de volgende code als basis voor uw web scraper:

```csharp
using System;
using System.Net.Http;
using HtmlAgilityPack;

namespace WebScraper
{
    class Program
    {
        static async System.Threading.Tasks.Task Main(string[] args)
        {
            var url = "https://www.nasa.gov/rss/dyn/breaking_news.rss"; // of een andere nieuws-URL van NASA of ESA

            var httpClient = new HttpClient();
            var html = await httpClient.GetStringAsync(url);

            var htmlDocument = new HtmlDocument();
            htmlDocument.LoadHtml(html);
            
            var htmlBody = htmlDocument.DocumentNode.SelectSingleNode("//rss//channel");

            var newsItems = htmlBody.SelectNodes(".//item");
            if(newsItems != null)
            {
                foreach (var item in newsItems)
                {
                    var title = item.SelectSingleNode(".//title").InnerText;
                    var pubDate = item.SelectSingleNode(".//pubDate").InnerText;
                    var description = item.SelectSingleNode(".//description").InnerText;

                    Console.WriteLine($"Titel: {title}");
                    Console.WriteLine($"Publicatiedatum: {pubDate}");
                    Console.WriteLine($"Samenvatting: {description}");
                    Console.WriteLine("----------------------------------------------------");
                }
            }
            else
            {
                Console.WriteLine("Geen nieuwsartikelen gevonden.");
            }
        }
    }
}
```

4. Voer het programma uit en controleer of de informatie correct wordt weergegeven.
   - Druk op 'Ctrl + F5' om het programma te starten zonder de debugger.
   - Het programma zou nu de titel, publicatiedatum en samenvatting van elk nieuwsartikel van NASA of ESA moeten weergeven.

Opmerking: Deze basiscode kan verder worden aangepast en verbeterd, afhankelijk van de vereisten van de opdracht. Zorg ervoor dat je de juiste URL voor de nieuwsfeed gebruikt en pas de scrapingcode aan als de structuur van de webpagina verandert.</AssignmentText><Config><Difficulty>Makkelijk</Difficulty><ExtraDescription/><ID>44</ID><Language>C#</Language><Reqs>Scraper voor een bekende datadienst website nieuws zoals NASA of ESA.</Reqs><Title>Web Scraper</Title></Config><ID>44</ID><RawResponseObj z:Id="i1" xmlns:a="http://schemas.datacontract.org/2004/07/Syn_OpenAI_CoreLib.Core.Models.Data" xmlns:z="http://schemas.microsoft.com/2003/10/Serialization/"><created xmlns="http://schemas.datacontract.org/2004/07/Syn_OpenAI_CoreLib.Data">1682887901</created><id xmlns="http://schemas.datacontract.org/2004/07/Syn_OpenAI_CoreLib.Data">chatcmpl-7B8JB8lEOsdIngELjDTNB4rVEdLHD</id><a:choices><a:API_AIResponse_ChoicesJSON><a:finish_reason>stop</a:finish_reason><a:index>0</a:index><a:message><a:content>Opdracht: Web Scraper

Titel: Web Scraper voor nieuws van NASA of ESA

Doel: Maak een eenvoudige web scraper die nieuwsartikelen van NASA of ESA ophaalt en de titel, publicatiedatum en samenvatting van elk artikel weergeeft.

Programmeertaal: C#

Moeilijkheidsgraad: Makkelijk

Inleiding: In deze opdracht ga je een eenvoudige web scraper bouwen die nieuwsartikelen van NASA of ESA verzamelt. Een web scraper is een programma dat gegevens van websites haalt en de verkregen gegevens structureert en opslaat voor analyse of andere doeleinden. Je gaat gebruikmaken van C# en de HtmlAgilityPack library om dit te realiseren.

Benodigdheden:
1. Visual Studio of een andere C#-ontwikkelomgeving.
2. HtmlAgilityPack (https://www.nuget.org/packages/HtmlAgilityPack/)

Stappen:
1. Maak een nieuw C# Console Application-project in Visual Studio.
   - Open Visual Studio en kies voor 'Create a new project'.
   - Zoek naar 'Console App (.NET Core)' en selecteer deze optie. Klik vervolgens op 'Next'.
   - Geef je project een naam en kies een locatie waar je het wilt opslaan. Klik op 'Create' om het project aan te maken.

2. Installeer HtmlAgilityPack via NuGet Package Manager.
   - Ga naar 'Tools' &gt; 'NuGet Package Manager' &gt; 'Manage NuGet Packages for Solution'.
   - Klik op 'Browse' en zoek naar 'HtmlAgilityPack'.
   - Selecteer 'HtmlAgilityPack' en installeer het in je project.

3. Gebruik de volgende code als basis voor uw web scraper:

```csharp
using System;
using System.Net.Http;
using HtmlAgilityPack;

namespace WebScraper
{
    class Program
    {
        static async System.Threading.Tasks.Task Main(string[] args)
        {
            var url = "https://www.nasa.gov/rss/dyn/breaking_news.rss"; // of een andere nieuws-URL van NASA of ESA

            var httpClient = new HttpClient();
            var html = await httpClient.GetStringAsync(url);

            var htmlDocument = new HtmlDocument();
            htmlDocument.LoadHtml(html);
            
            var htmlBody = htmlDocument.DocumentNode.SelectSingleNode("//rss//channel");

            var newsItems = htmlBody.SelectNodes(".//item");
            if(newsItems != null)
            {
                foreach (var item in newsItems)
                {
                    var title = item.SelectSingleNode(".//title").InnerText;
                    var pubDate = item.SelectSingleNode(".//pubDate").InnerText;
                    var description = item.SelectSingleNode(".//description").InnerText;

                    Console.WriteLine($"Titel: {title}");
                    Console.WriteLine($"Publicatiedatum: {pubDate}");
                    Console.WriteLine($"Samenvatting: {description}");
                    Console.WriteLine("----------------------------------------------------");
                }
            }
            else
            {
                Console.WriteLine("Geen nieuwsartikelen gevonden.");
            }
        }
    }
}
```

4. Voer het programma uit en controleer of de informatie correct wordt weergegeven.
   - Druk op 'Ctrl + F5' om het programma te starten zonder de debugger.
   - Het programma zou nu de titel, publicatiedatum en samenvatting van elk nieuwsartikel van NASA of ESA moeten weergeven.

Opmerking: Deze basiscode kan verder worden aangepast en verbeterd, afhankelijk van de vereisten van de opdracht. Zorg ervoor dat je de juiste URL voor de nieuwsfeed gebruikt en pas de scrapingcode aan als de structuur van de webpagina verandert.</a:content><a:role>assistant</a:role></a:message></a:API_AIResponse_ChoicesJSON></a:choices><a:model>gpt-4-0314</a:model><a:object>chat.completion</a:object><a:usage><a:completion_tokens>826</a:completion_tokens><a:prompt_tokens>722</a:prompt_tokens><a:total_tokens>1548</a:total_tokens></a:usage></RawResponseObj></Assignment>